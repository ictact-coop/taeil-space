# 전태일기념관 대관 시스템 — 개발 계획 v0.2

> 작성일 2026-09-25 · 기준 문서
> - 대관 시스템 요구사항 정의서 v1.0 (2026-09-15) — 이하 **[요구]**
> - 대관 시스템 와이어프레임 v1.1 — 이하 **[WF]**
> - 대관 시스템 사이트 디자인 시안 v1.0 (2026-09-25) — 이하 **[시안]**
>
> **변경 이력**
> | 버전 | 일자 | 내용 |
> |---|---|---|
> | v0.1 | 2026-09-25 | 최초 작성 |
> | v0.2 | 2026-09-25 | 발주처 결정 3건 반영: ① 단체는 고유번호·사업자등록번호로 판별 ② **신청할 때 결제하고, 그 뒤에 관리자가 승인**하는 흐름으로 변경 ③ 공휴일·휴관일을 모두 관리자 설정으로 |

---

## 0. 요약

| 항목 | 내용 |
|---|---|
| 목표 | 2026년 10월 유료 대관 전환. **신청 + 결제 → 관리자 심사·승인 → 예약확정** (반려되면 자동 전액 환불) |
| MVP 범위 | 안내·공간·달력·신청·자동검증·결제·심사·승인·예약확정·취소/환불·관리자 캘린더·결제/정산·이메일/LMS ([요구] 27장) |
| 가장 큰 위험 | ① 오늘(9/25) 기준 10월 말까지 약 5주이고, 추석·개천절·한글날 연휴가 끼어 있음 ② 정책 P-01~P-12가 아직 정해지지 않음 ③ **결제가 신청 단계로 앞당겨져서, PG 계약(보통 1~3주)이 늦어지면 신청 접수 자체를 시작할 수 없음** |
| 권장 전략 | **2단계 오픈**: 10월 중에 *계좌이체 + 담당자 입금 확인* 방식으로 신청·심사·예약을 먼저 시작하고(1단계), PG 자동결제·자동환불은 11월 중순에 붙임(2단계). 정책은 코드에 박지 않고 관리자 설정값으로 둠 |
| 기술 스택(제안) | Next.js(TypeScript) 모놀리스 + PostgreSQL(범위 배제 제약으로 이중 예약 차단) + pg-boss(예약 작업) + S3 호환 저장소 + PortOne(PG 통합 결제) |

---

## 1. 문서 분석 결과

### 1.1 핵심 도메인 규칙 (구현 시 반드시 지킬 것)

1. **신청은 결제가 끝나야 접수된다.** 이용자가 신청서를 제출하면 곧바로 결제 단계로 넘어가고, 결제가 완료되어야 "신청접수"가 된다. 전액 감면(0원)인 건은 PG를 거치지 않고 바로 신청접수된다.
2. **예약확정은 관리자 승인 시점에 생긴다.** 이미 결제된 신청을 관리자가 심사해서 승인하면 예약확정, 반려하면 **자동으로 전액 환불**된다. *([요구] 3·6·7장의 "승인 → 결제 → 확정"과 반대 순서이며, 발주처 결정 v0.2에 따른 것)*
3. **결제를 시작하는 순간부터 일정을 임시 확보한다.** 결제 유효시간(정책값, 예: 30분) 동안만 확보하고, 그 안에 결제하지 않으면 자동으로 풀어준다. 결제된 신청은 심사가 끝날 때까지 확보를 유지하고, 승인되면 확정, 반려·철회되면 해제한다. 따라서 **같은 공간·같은 시간에는 먼저 결제한 신청 한 건만** 심사에 올라간다.
4. **같은 공간·같은 시간의 확보·확정·자체행사·휴관이 겹치는 것은 DB 수준에서 막는다**(8장, AT-10).
5. **결제완료는 PG 웹훅이나 서버 측 결제 조회로만 판정한다.** 브라우저가 돌려주는 결과만 믿지 않는다. 주문번호 단위로 멱등성을 보장한다(17장).
6. **금액 스냅샷**: 결제할 때 계산한 금액을 신청 건에 남기고, 요금표는 버전으로 관리한다(16장, [WF] 12쪽).
7. **감사 로그**: 관리자가 상태·금액·환불을 수동으로 바꾸면 변경 전후 값, 처리자, 시각, 사유를 남긴다(8장, AT-11).
8. **단체는 고유번호 또는 사업자등록번호로 판별한다.** BR-03(단체당 1일 1건)과 BR-04(6개월 횟수)는 이 번호를 기준으로 센다(아래 2.5).
9. **공휴일·휴관일은 모두 관리자 설정이다.** BR-01("매주 월요일, 1월 1일, 설·추석 당일")을 코드에 넣지 않고, 설정 데이터의 초기값으로만 넣는다(아래 2.6).
10. **나머지 정책도 설정값으로 둔다**: 신청기한, 1일 1건, 6개월 횟수, 시간 단위, 완충시간, 결제 유효시간, 환불률 등([WF] 14쪽 설계 원칙).

### 1.2 문서끼리 맞지 않는 부분 — 상세설계 전에 정리 필요

| # | 내용 | 근거 | 제안 / 상태 |
|---|---|---|---|
| G-1 | **화면 ID 체계가 다름.** [요구]는 U-01~U-10, A-01~A-12, [WF]는 U-01~U-07, A-01~A-06 | [요구] 19·20장 / [WF] 2쪽 | [요구] 체계로 통일, 부록 A 매핑표 |
| G-2 | **시간 선택 방식.** [WF]·[시안]은 고정 블록(10–13 / 14–17 / 18–21), [요구]는 공간별 시간 단위·최소시간·완충시간 설정 | [WF] 4쪽, [요구] 11장, P-02 | 데이터 모델은 "시작·종료 시각 + 공간별 단위"로 일반화하고, UI는 설정에 따라 블록형·연속형 둘 다 지원 |
| G-3 | **동의 항목 개수.** [WF]는 4개를 각각 동의, [시안] 이미지는 2개로 합침 | [WF] 6쪽, [시안] 4쪽 | **항목별 분리 동의** 채택 |
| G-4 | **공간 카드 누락.** [WF] U-01에 회의실2가 없음 | [WF] 3쪽 | 5개 공간 모두 노출 |
| G-5 | **정책 결정 목록이 다름.** [요구]는 12개, PPT는 8개. P-11·P-12가 PPT에 없음 | [요구] 28장 | 결정표 하나로 통합(6장) |
| G-6 | 비회원의 단체 식별 기준 | [요구] 3·10장 | ✅ **해결(v0.2)**: 고유번호 또는 사업자등록번호로 판별(2.5) |
| G-7 | **상태 전이가 빠진 경우가 있음.** 이용자 철회, 보완기한 만료, 기념관 사정 취소, 환불률 0% 취소, 환불 실패 후 재처리 | [요구] 7·18장 | 2.3 상태 모델에 반영 |
| G-8 | 공휴일 데이터 출처 | [요구] 10장 | ✅ **해결(v0.2)**: 모든 휴관 규칙을 관리자 설정으로(2.6) |
| G-9 | **동절기 운영.** 관람 종료 17:30과 야간 대관(18:00~21:00)의 관계 | [요구] 11장 | 공간 운영시간을 "기간별 운영시간표"로 모델링 |
| G-10 | **결제·승인 순서를 바꿨으므로 기준 문서 개정 필요** | [요구] 3·4·6·7·17·21·26장, [WF] 2·7·10쪽, [시안] 1·5·6쪽 | 요구사항 정의서 v1.1 개정과 와이어프레임 수정 필요(부록 B) |

### 1.3 [시안]에서 가져올 디자인 기준

- 색상 토큰: Deep Navy `#153F56`, Brick Orange `#C94F32`(주요 행동·강조), Warm Cream `#F5F1E8`(배경·요약 패널), Status Green `#34725E`(확정·통과)
- 제목은 명조 계열로 인상 있게, 입력·상태·금액은 고딕 계열로 빠르게 읽히게
- 레이아웃: 신청 단계는 **3열(공간 목록 | 달력·시간 | 선택 요약 고정)**, 신청서는 **2열 폼 + 요약 패널**, 관리자 심사는 **목록과 상세를 한 화면에 나란히**
- 상태는 색만으로 구분하지 않고 문자·아이콘을 함께 씀(NFR-02)
- 이용자 4단계 표시를 **① 공간·일정 선택 ② 신청정보 입력 ③ 결제 ④ 관리자 심사·확정**으로 바꿈([시안] 3쪽의 "03 관리자 심사 → 04 결제·예약확정" 순서 변경)

---

## 2. 시스템 설계 방향

### 2.1 기술 스택 (제안 — 저장소가 비어 있어 새로 정함)

| 영역 | 선택 | 이유 |
|---|---|---|
| 앱 | **Next.js (App Router) + TypeScript**, 이용자·관리자를 하나의 앱에서 운영(`/`, `/admin`) | 소규모 운영 조직에 맞게 배포 단위를 하나로. 라우트 핸들러로 PG 웹훅 처리 |
| DB | **PostgreSQL 16** + `btree_gist` | `EXCLUDE USING gist (space_id WITH =, during WITH &&)`로 이중 예약을 DB가 직접 차단 |
| ORM | Drizzle ORM (+ 배제 제약은 SQL 마이그레이션) | SQL에 가깝게 쓸 수 있고, 제약·트랜잭션을 명시적으로 제어 가능 |
| 작업 큐 | **pg-boss** (Postgres 기반) | 결제 유효시간 만료, 보완기한 만료, 반려 시 환불 재시도, 이용완료 처리, 개인정보 파기 배치 |
| 결제 | **PortOne V2** (그 아래 PG사는 계약에 따라 선택) | PG사가 정해지기 전에도 연동 개발 가능. 결제·조회·취소(부분환불)·웹훅 서명 지원 |
| 알림 | 이메일: AWS SES 또는 NHN Cloud / LMS: NHN Cloud·솔라피 등 | 공급자를 바꿀 수 있는 어댑터 구조. 알림톡은 2차 |
| 파일 | S3 호환 객체 저장소 + 서명된 URL + ClamAV 검사 | 첨부파일 악성 여부 검사·접근 제어 |
| 인증 | 이용자: 휴대전화/이메일 OTP + 신청번호, 관리자: 아이디·비밀번호 + TOTP 2단계 인증 | [요구] 3장, NFR-05 |
| 인프라 | 국내 리전(AWS 서울 또는 NCP), 관리형 DB, 매일 백업 + PITR | 개인정보 국내 보관, NFR-07 |
| 품질 | Vitest(도메인 로직), Playwright(E2E·모바일 뷰포트), axe(접근성) | 인수 테스트(5장)를 자동 테스트로 만들어 둠 |

### 2.2 모듈 구성

```
src/
  domain/            # 프레임워크와 무관한 순수 도메인 로직(테스트 대상 1순위)
    rules/           #   BR-01~08 규칙 엔진 (입력: 신청안 + 정책·휴관 설정 → 위반 목록)
    calendar/        #   휴관 규칙 계산(정기 요일·매년 고정일·특정일·예외 개관일)
    organization/    #   고유번호·사업자등록번호 정규화·검증
    pricing/         #   요금 계산(요금표 버전, 감면, 스냅샷)
    refund/          #   환불률·환불 가능 금액 계산(승인 전 / 승인 후 / 반려 / 기념관 사정)
    reservation/     #   상태 머신·전이 가드
  server/
    db/              # 스키마·마이그레이션(배제 제약 포함)
    payments/        # PortOne 어댑터, 웹훅 검증, 결제 조회 재확인, 자동 환불
    notifications/   # 템플릿 렌더링, 이메일/LMS 어댑터, 발송 이력
    jobs/            # pg-boss 작업
    audit/           # 감사 로그 기록기
  app/
    (public)/        # U-01~U-10
    admin/           # A-01~A-12
```

### 2.3 상태 모델 (v0.2: 신청 시 결제 → 승인 → 확정)

```
작성중 ─제출→ 결제대기 ─┬─결제성공(웹훅/조회)→ 신청접수
   (일정 임시확보,      ├─(0원) 제출 즉시→ 신청접수
    결제 유효시간)      └─유효시간 만료·결제 포기→ 미결제취소 (일정 해제)

신청접수 ─담당자 확인→ 검토중 ─┬─보완요청→ 보완요청 ─재제출→ 검토중
                              │                 └─보완기한 만료→ 신청종료* → 전액 환불
                              ├─반려→ 반려 → 전액 자동 환불
                              └─승인→ 예약확정

신청접수·검토중·보완요청 ─이용자 철회→ 신청취소* → 승인 전 환불 규정(기본 전액)
예약확정 ─이용자 취소→ 취소요청 ─┬─환불성공→ 환불완료
                                 └─환불률 0%→ 취소완료*
예약확정 ─기념관 사정 취소(관리자, 사유 필수)→ 전액 환불 → 환불완료
예약확정 ─이용 종료(배치)→ 이용완료
(* 는 요구사항에 없어 새로 제안한 상태)
```

- **신청 상태와 결제·환불 상태는 분리해서 관리한다.** 신청은 "반려"로 끝나도 환불은 `refunds` 테이블에서 요청 → 성공/실패로 따로 진행하고, 실패하면 A-06 재처리 목록에 나타난다. 이렇게 해야 "반려했는데 환불이 안 된" 건을 놓치지 않는다.
- 상태를 바꾸는 코드는 `reservation/transition()` 한 곳에서만 실행하고, 같은 트랜잭션 안에서 **감사 로그 기록 + 알림·환불 작업 등록(아웃박스)**까지 처리한다.
- 일정 점유 테이블 `slot_occupancies(space_id, during tstzrange, kind, application_id, expires_at)`
  - `kind ∈ {PENDING_PAYMENT(결제대기, 만료 시각 있음), HELD(결제완료·심사중), CONFIRMED(예약확정), BLOCK(자체행사·점검·임시차단)}` 행에 배제 제약을 건다.
  - 만료된 PENDING_PAYMENT 행은 배치가 지우고, 새 신청이 들어올 때도 같은 트랜잭션 안에서 만료분을 먼저 정리해서 일정이 필요 이상 막히지 않게 한다.
  - 이용자 달력에서는 PENDING_PAYMENT·HELD를 "신청진행", CONFIRMED를 "예약완료", BLOCK과 휴관일을 "대관불가"로 표시한다(단체명은 공개하지 않음).
  - 준비·철수 완충시간은 `during` 범위를 앞뒤로 늘려서 저장한다.
- **보완요청 때 고칠 수 있는 범위**: 이미 결제한 금액과 맞아야 하므로 기본적으로 *금액이 바뀌지 않는 항목*(행사 내용, 첨부, 연락처, 인원(정원 안에서))만 고칠 수 있게 한다. 공간·일시를 바꾸려면 철회하고 다시 신청해야 한다(P-14).

### 2.4 주요 엔터티 ([요구] 22장 기반)

`spaces`, `space_operating_hours`(기간별), `closure_rules`(휴관 규칙, 2.6), `schedule_blocks`, `booking_windows`(교육실 접수 공개기간), `policies`(키-값 + 적용기간), `fee_tables` / `fee_items`(버전), `discount_rules`, `organizations`(고유번호·사업자등록번호, 2.5), `applicants`(인증된 연락처), `applications`(→ organization_id), `application_status_history`, `slot_occupancies`, `price_snapshots`, `attachments`, `payments`(신청 1건에 여러 번 시도 가능), `refunds`, `notification_templates`, `notification_logs`, `admin_users` / `roles`, `audit_logs`(추가만 가능, 수정·삭제 불가), `pii_access_logs`

### 2.5 단체 판별 (고유번호·사업자등록번호)

- **입력**: 신청서에 "고유번호 또는 사업자등록번호" 칸을 두고, 번호 종류(고유번호 / 사업자등록번호)를 고르게 한다. 두 번호 모두 10자리(`XXX-XX-XXXXX`) 형식이다.
- **검증**: 하이픈을 빼고 숫자 10자리로 정규화한 뒤 형식을 검사하고, 사업자등록번호는 검증번호(마지막 자리 체크섬)도 확인한다. 고유번호에도 같은 체크섬을 적용할지는 실제 번호 샘플로 확인하고 정한다. 국세청 사업자등록 상태조회 API(공공데이터포털)로 휴·폐업 여부를 확인하는 기능은 선택 사항이다.
- **판별**: `organizations.reg_no`(정규화된 번호)에 유일성 제약을 건다. 같은 번호로 들어온 신청은 같은 단체로 보고 BR-03(같은 날 1건)과 BR-04(6개월 횟수)를 센다. 단체명은 신청마다 달라도 되는 표시용 값으로만 쓴다.
- **번호가 없는 경우**(개인, 임의단체 등): 번호를 필수로 할지는 P-03에서 정한다. 필수가 아니라면 번호 없는 신청은 자동 판별을 하지 않고, 심사 화면에 "단체 번호 없음 — 중복 여부 수동 확인"으로 표시한다. 이때 같은 인증 휴대전화로 들어온 이전 신청을 참고용으로 함께 보여준다.
- **관리자 기능**: 심사 화면(A-04)에 같은 번호의 이전 신청·이용 내역과 6개월 사용 횟수를 보여준다. 번호를 잘못 입력한 건은 관리자가 고칠 수 있고, 이 수정은 감사 로그에 남긴다.

### 2.6 공휴일·휴관일 설정

BR-01을 코드에 고정하지 않고, 관리자가 A-09 일정 관리에서 다음 규칙을 등록한다.

| 규칙 유형 | 예시 | 적용 범위 |
|---|---|---|
| 정기 휴관 요일 | 매주 월요일 | 전체 또는 공간별 |
| 매년 같은 날 | 1월 1일 | 전체 또는 공간별 |
| 특정 날짜·기간 | 2027 설 당일, 추석 당일, 임시 휴관, 시설 점검 기간 | 전체 또는 공간별 |
| 예외 개관일 | "월요일이지만 이날은 개관" | 위 규칙들보다 우선 |

- 각 규칙에 이름, 사유, 적용 시작·종료일, 이용자에게 보여줄 문구("휴관일입니다" 등)를 둔다. 이용자가 해당 날짜를 누르면 이 문구가 나온다(AT-01).
- 우선순위: 예외 개관일 > 특정 날짜·기간 > 매년 같은 날 > 정기 휴관 요일.
- 시간 단위 차단(자체행사·점검 몇 시간)은 기존 `schedule_blocks`로, 하루 전체 휴관은 `closure_rules`로 처리한다.
- **초기 데이터**: 월요일 휴관, 1월 1일, 2026~2027년 설·추석 당일을 시드로 넣는다. 공공데이터포털 특일 정보 API에서 연도별 공휴일을 불러와 **관리자가 확인한 뒤 등록**하는 버튼은 선택 기능이다(자동 등록은 하지 않음).
- 이미 신청·확정된 날짜에 휴관 규칙을 새로 넣으면 저장 전에 겹치는 신청 목록을 경고로 보여준다. 기존 신청은 자동으로 취소하지 않는다.

---

## 3. 일정 계획

> 2026년 연휴(확인 필요): 추석 9/24~9/26, 대체공휴일 9/28(월), 개천절 대체공휴일 10/5(월), 한글날 10/9(금).
> 결제가 신청 단계로 앞당겨졌으므로 **PG 가입 신청은 S0 첫날에** 해야 한다.

### 3.1 권장안 — 2단계 오픈

| 스프린트 | 기간 | 개발 | 발주처(기념관) 할 일 | 완료 조건 |
|---|---|---|---|---|
| **S0 준비** | 9/29–10/2 | 저장소·CI·스테이징 환경, DB 스키마 초안(점유·휴관 규칙·단체), 디자인 토큰, 규칙 엔진 뼈대 | **PG 가입 신청(최우선)**, P-01·P-03·P-06·P-14·P-15 1차 확정 워크숍, 이메일 발신 도메인·LMS 발신번호 등록 | 요구사항 추적표 v1, 상태 모델 합의 |
| **S1 신청** | 10/6–10/16 | U-01~U-05(안내·공간·달력·일시선택·신청서), BR-01~08 규칙 엔진, **휴관 규칙 설정(A-09)**, **단체번호 검증·판별**, 첨부파일, 이메일 알림, A-02 캘린더, A-04 심사(보완·반려·승인) | 공간 사진·문구·이용규정 전문, 2026~2027 휴관일 목록 제공, P-02·P-07·P-08 확정 | AT-01~05 통과 |
| **S2 1단계 오픈 준비** | 10/19–10/23 | **결제대기(계좌이체 입금 기한) → 담당자 입금확인 → 신청접수**, 반려·철회 시 환불계좌 입력과 수동 환불 처리, 결제 유효시간 만료 배치, U-06·U-07·U-08 나의 대관(OTP), LMS, A-01 대시보드, A-12 계정·권한·감사로그 | 운영 교육, 시범 신청 | AT-06·08·10·11·14 통과(수동 결제 기준) |
| **🚩 1단계 오픈** | **10/26(월) 목표** | 유료 대관 접수 시작. 신청 제출 뒤 안내된 계좌로 입금하면 담당자가 확인해서 "입금확인"을 누르고(감사 로그), 그 뒤 심사·승인 | 장애 대응 연락망 가동 | — |
| **S3 PG 연동** | 10/26–11/6 | U-09 PG 결제(신청서 제출 직후), 웹훅·결제 조회 재확인·멱등성, **반려·보완기한 만료 시 자동 전액 환불**, U-10 취소·환불(부분환불 포함), A-06 결제·환불·재처리 목록 | PG 심사 완료, P-06·P-09·P-10 최종 확정 | AT-07·09·13 통과(테스트 환경) |
| **S4 정산·안정화** | 11/9–11/13 | A-07·A-08 공간·요금·감면 관리 화면, A-10 통계·정산 CSV, A-11 알림 템플릿, 개인정보 파기 배치, 보안 점검(OWASP), 백업·복구 시험 | 실결제 소액 테스트(결제 → 반려 → 환불 포함), P-11·P-12 확정 | 인수 테스트 전부 통과 |
| **🚩 2단계 오픈** | **11/16(월) 목표** | PG 자동결제 전환, 모니터링 | — | — |

*S0~S3 동안 요금표·감면은 시드 데이터로 넣어두고, 관리 화면은 S4에 제공한다. 휴관 규칙 설정은 오픈 전에 반드시 필요하므로 S1에 넣었다.*

*1단계(계좌이체) 주의: 입금 기한 동안 일정을 확보해야 하므로 결제 유효시간을 PG 방식(예: 30분)보다 길게(예: 24시간) 따로 설정한다. 반려·철회 시 계좌로 환불해야 하므로 환불계좌를 입력받는다.*

### 3.2 대안 — 한 번에 오픈

10월 안에 PG까지 모두 갖추려면 **9/30까지 P-01·P-06·P-09 확정 + PG 계약 완료**가 필요하다. 결제 순서가 앞당겨져 PG가 신청 접수의 전제 조건이 되었으므로, 계약이 늦어지면 오픈 자체가 밀린다. 현실적인 목표는 11월 중순이다.

---

## 4. 작업 분해 (요구사항 ID 기준)

### 4.1 이용자

| 작업 | 요구 ID | 화면 | 스프린트 | 비고 |
|---|---|---|---|---|
| 대관 안내(절차·시간·요금·규정·환불) | USR-001 | U-01 | S1 | 절차 문구를 "신청·결제 → 심사 → 확정"으로, 반려 시 전액 환불 안내 포함 |
| 공간 목록·상세 | USR-002 | U-02, U-03 | S1 | 5개 공간 시드 데이터 |
| 일정 달력(4가지 상태, 단체명 비공개) | USR-003 | U-03 | S1 | 휴관 규칙·차단·점유를 합쳐서 계산, 색 + 문자 표시 |
| 일시 선택 + 즉시 자동검증 | USR-004 | U-04 | S1 | 규칙 엔진을 클라이언트·서버에서 공용으로 사용 |
| 요금 계산(항목별) | USR-005 | U-04 | S1 | 결제 직전에 서버에서 다시 계산해서 스냅샷 저장 |
| 신청서 + 첨부 + 분리 동의 + **단체번호** | USR-006 | U-05 | S1 | 번호 형식·검증번호 즉시 검사, 야간이면 출입문 관리자 입력 필수 |
| 임시저장 | USR-007 | U-05 | 보류 | 선택 기능 |
| **결제(신청서 제출 직후)** | USR-009 | U-09 | S2(계좌이체) / S3(PG) | 결제 유효시간 표시, 만료 시 일정 해제 안내 |
| 신청 완료(결제 완료 후) | — | U-06 | S2 | 신청번호, 예상 심사기간 3~7일, 반려 시 환불 안내 |
| 보완 제출 | USR-008 | U-08 | S1 | 금액이 바뀌지 않는 항목만 수정 가능(2.3) |
| 나의 대관(OTP 인증) | USR-010 | U-07, U-08 | S2 | 처리 이력 타임라인(신청·결제·심사·환불) |
| 철회·취소·환불 신청 | USR-011 | U-10 | S3 | 승인 전 철회와 확정 후 취소의 환불 규정을 구분해서 예상 환불액 표시 |
| 영수증·예약확인 조회 | USR-012 | U-08 | S3 | PG 영수증 링크 + 예약확인 인쇄용 화면(확정 후) |

### 4.2 관리자

| 작업 | 요구 ID | 화면 | 스프린트 |
|---|---|---|---|
| 대시보드(신규 신청·입금 확인 대기·검토중·취소요청·환불 실패) | ADM-001 | A-01 | S2 |
| 대관 캘린더(월/주/일, 필터) | ADM-002 | A-02 | S1 |
| 신청 목록·심사(자동검증 결과, 결제 상태, 같은 단체번호 이력, 내부 메모, 보완·반려(자동 환불)·승인) | ADM-003 | A-03, A-04 | S1 |
| 예약 관리·수동 등록·변경 | ADM-004 | A-05 | S2 |
| **휴관 규칙 설정**·일정 차단·교육실 접수기간 | ADM-005 | A-09 | S1 |
| 공간 관리 | ADM-006 | A-07 | S4 (그 전까지는 시드) |
| 요금표 관리(버전) | ADM-007 | A-08 | S4 (그 전까지는 시드) |
| 감면 관리 | ADM-008 | A-08 | S4 |
| 결제·환불(입금 확인, 환불 재처리) | ADM-009 | A-06 | S2(수동) / S3(PG) |
| 알림 템플릿·재발송 | ADM-010 | A-11 | S4 (그 전까지는 코드 템플릿) |
| 통계·정산·CSV | ADM-011 | A-10 | S4 |
| 권한·감사 로그 | ADM-012 | A-12 | S2 |

### 4.3 공통·비기능

| 작업 | 근거 | 스프린트 |
|---|---|---|
| 배제 제약 기반 이중 예약 방지 + 동시성 테스트 | 8장, AT-10 | S0–S1 |
| 감사 로그(추가만 가능) + 개인정보 조회·다운로드 이력 | 8·24장, NFR-06 | S2 |
| 결제 유효시간 만료·보완기한 만료·환불 재시도·이용완료 배치 | 2.3 | S2–S3 |
| KST로 표시, UTC로 저장 | NFR-09 | S0 |
| 반응형·접근성(키보드, 레이블, 색 외 상태 표시) | NFR-01·02 | 전 기간 |
| 보안: 관리자 TOTP, 역할별 권한, 비밀정보 관리, 속도 제한(OTP) | NFR-05 | S2 |
| 백업·복구 시험, 오류 모니터링(Sentry 등) | NFR-04·07 | S4 |
| 개인정보 보존기간 만료 시 파기·비식별화 배치 | 24장, NFR-10 | S4 |

---

## 5. 테스트·검수 전략

- **도메인 단위 테스트**: 규칙 엔진(BR-01~08), 휴관 규칙 우선순위, 단체번호 정규화·검증, 요금 계산, 환불 계산, 상태 전이 가드. 정책값을 바꿔 가며 테스트한다.
- **DB 동시성 테스트**: 같은 슬롯에 결제 시작 요청을 동시에 N건 보내도 1건만 일정을 확보하는지.
- **결제 시나리오**: 웹훅 먼저 도착 / 브라우저 복귀 먼저 / 웹훅 중복 / 웹훅 유실(→ 조회로 재확인) / 결제 유효시간이 끝난 뒤 늦게 결제 성공(→ 일정이 이미 풀렸으면 자동 환불) / 반려 후 자동 환불 실패 → 재처리 / 부분환불 누적 한도.
- **E2E(Playwright)**: 인수 테스트를 시나리오로 자동화하고, 모바일 뷰포트에서 조회 → 신청 → 결제 → 승인 → 확인까지 한 번에 확인.
- **요구사항 추적표**: `docs/traceability.md`에서 [요구] ID ↔ 화면 ↔ API ↔ 테스트를 관리([요구] 30장 산출물).

**인수 테스트 변경안** ([요구] 26장 기준, v0.2 흐름 반영)

| ID | 합격 기준 | 변경 |
|---|---|---|
| AT-01 | 관리자가 설정한 휴관일(정기·매년·특정일)을 선택할 수 없고 이유가 표시된다. 예외 개관일은 선택할 수 있다 | 수정 |
| AT-02~05 | [요구]와 같음 | — |
| AT-06 | 신청서를 제출하면 결제 단계로 넘어가고 결제 유효시간 동안 일정이 임시 확보된다 | 수정 |
| AT-07 | PG 결제에 성공하면 신청접수가 단 한 건만 생긴다 | 수정 |
| AT-08 | 결제 유효시간이 지나면 자동취소되고 일정이 다시 열린다 | 수정 |
| AT-09~12 | [요구]와 같음 (AT-12는 "조회 → 신청 → 결제 → 승인 → 확인"으로) | 일부 수정 |
| AT-13 | 반려하면 전액 자동 환불되고, PG 결과와 신청·환불 상태가 일치한다 | 신규 |
| AT-14 | 같은 고유번호·사업자등록번호로 같은 날 두 번째 신청을 하면 막히고, 6개월 횟수를 넘기면 안내와 함께 막힌다 | 신규 |

---

## 6. 정책 결정 요청표 (발주처)

| ID | 결정사항 | 개발에 미치는 영향 | 필요 시점 | 결정 전 개발 기본값 |
|---|---|---|---|---|
| P-01 | 공간별 기본·추가·야간 요금 | 요금 계산·결제 금액 | S1 중반 | 요금표 구조만 구현, 금액은 시드 |
| P-02 | 시간 단위·완충시간 | 달력 UI, 점유 범위 | S1 시작 | 1시간 단위, 완충 0분 |
| P-03 | 6개월 횟수·산정법, **단체번호를 필수로 할지** | BR-03·04 | S0 | 번호 선택 입력, 번호가 있으면 자동 판별 / 없으면 수동 확인 |
| P-04 | ~~접수·검토 단계 동시신청 허용~~ → **v0.2에서 정리됨**: 먼저 결제한 신청이 일정을 확보(동시신청 없음) | — | — | — |
| P-05 | **결제 유효시간**(PG / 계좌이체 각각), 연장 가능 여부 *(v0.2: "승인 후 48시간 결제기한"을 대체)* | 점유·배치 | S2 | PG 30분, 계좌이체 24시간 |
| P-06 | 확정 후 취소 시점별 환불률·기념관 귀책 | 환불 계산 | S3 | 환불률 표 설정 화면만 제공 |
| P-07 | 감면 대상·비율·증빙·승인권자 | 요금·심사 | S1 | 비율형 감면 1종, 증빙 필수 |
| P-08 | 교육실 접수기간 자동 확장/수동 공개 | 달력 | S1 | 수동 공개 + "1주 연장" 버튼 |
| P-09 | PG사·결제수단·부가세·영수증·세금계산서 | PG 연동 | **S0 (계약 신청)** | PortOne 테스트 채널로 개발 |
| P-10 | 확정 후 취소의 환불 자동처리 여부·관리자 승인 범위 | 환불 흐름 | S3 | 관리자 승인 후 환불 (반려 환불은 항상 자동) |
| P-11 | 개인정보·첨부·결제자료 보존기간 | 파기 배치 | S4 | 설정값으로 둠 |
| P-12 | 기존 무료 신청 이관 범위·10월 경계 처리 | 데이터 이관 | S2 | 관리자가 수동 등록 |
| P-13 | 알림 채널·발송 시점 | 알림 | S1 | 부록 B 알림표 |
| P-14 | **(신규)** 보완요청 때 공간·일시 변경 허용 여부, 금액이 바뀌면 추가 결제·차액 환불을 할지 | 보완 흐름 | S1 | 금액이 바뀌는 수정은 불가, 철회 후 재신청 |
| P-15 | **(신규)** 승인 전 이용자 철회 시 환불률(수수료 공제 여부) | 환불 계산 | S2 | 전액 환불 |
| P-16 | **(신규)** 감면을 신청했는데 심사에서 인정되지 않을 때: 반려 / 차액 추가결제 요청 | 심사·결제 | S1 | 반려(전액 환불) 후 재신청 안내 |

---

## 7. 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| PG 심사 지연 | 신청 접수 자체가 막힘(v0.2에서 영향 커짐) | 1단계는 계좌이체 + 입금 확인으로 운영, PortOne 추상화로 PG사가 바뀌어도 코드 변경 최소화 |
| 반려·철회 환불 건수 증가 | 결제 수수료·민원, 환불 누락 | 신청 전 자동검증을 강하게(부적격 신청을 결제 전에 차단), 환불 상태를 별도 관리하고 실패 건은 재처리 목록과 대시보드에 노출 |
| 결제 후 긴 심사 기간 동안 일정 점유 | 인기 일정이 심사 중에 묶임 | 심사 기한(예: 결제 후 7일) 경과 건을 대시보드에서 강조, 필요하면 자동 알림 |
| 정책 확정 지연 | 재작업 | 모든 정책을 설정값으로 두고 "결정 전 기본값"으로 개발 계속 |
| 이중 예약·중복 결제 | 민원·환불 | DB 배제 제약, 주문번호 멱등성, 웹훅 서명 검증 + 결제 조회 재확인, 유효시간 이후 늦은 결제는 자동 환불 |
| 짧은 일정 속 범위 확대 | 오픈 지연 | 2차 고도화 항목([요구] 27장)은 백로그로 고정, 변경은 부록 A 절차로만 받음 |
| 개인정보 사고 | 법적 위험 | 최소 수집, 역할별 조회 권한, 조회 이력, 첨부파일 서명 URL·만료, 파기 배치 |

---

## 8. 바로 다음 할 일

1. **PG 가입 신청을 바로 시작한다**(결제가 신청 접수의 전제 조건이 됨).
2. 요구사항 정의서 v1.1과 와이어프레임을 부록 B대로 고친다.
3. 9/29 주 정책 워크숍: P-01·P-03·P-06·P-09·P-14·P-15·P-16을 우선 정한다.
4. S0 작업 시작: 저장소 뼈대(Next.js + Drizzle + Postgres + pg-boss), 스키마(점유·휴관 규칙·단체)·배제 제약 마이그레이션, 규칙 엔진 단위 테스트, 디자인 토큰.

---

## 부록 A. 화면 ID 매핑 ([요구] ↔ [WF])

| [요구] | 화면명 | [WF] |
|---|---|---|
| U-01 | 대관 안내 | U-01 |
| U-02 | 공간 목록 | U-01 (공간 카드) |
| U-03 | 공간 상세/달력 | U-02 |
| U-04 | 일시·옵션 선택 | U-02 |
| U-05 | 신청서 | U-03 (+U-04 동의) |
| U-06 | 신청 완료 | U-04 (**v0.2: 결제 완료 뒤에 표시**) |
| U-07 | 본인 확인/나의 대관 | — (와이어프레임 없음) |
| U-08 | 예약 상세 | U-06 |
| U-09 | 결제 | U-05 (**v0.2: 신청서 제출 직후로 이동**) |
| U-10 | 취소·환불 | U-06 (오른쪽) |
| A-01 | 대시보드 | A-01 |
| A-02 | 대관 캘린더 | A-01 |
| A-03 | 신청 목록 | A-02 |
| A-04 | 신청 상세/심사 | A-02 |
| A-05 | 예약 목록/상세 | — (와이어프레임 없음) |
| A-06 | 결제·환불 | A-05 |
| A-07 | 공간 관리 | A-06 |
| A-08 | 요금·감면 관리 | A-04 |
| A-09 | 일정 관리(휴관 규칙 포함) | A-03 |
| A-10 | 통계·정산 | — (와이어프레임 없음) |
| A-11 | 알림 템플릿 | A-06 |
| A-12 | 계정·권한·로그 | A-06 |

→ **U-07, A-05, A-10은 와이어프레임을 추가로 그려야 한다.**

## 부록 B. v0.2 결정에 따른 기준 문서 수정 목록

| 문서 | 위치 | 현재 | 수정 |
|---|---|---|---|
| [요구] | 표지·1장 핵심 운영안 | 신청 → 심사·승인 → 결제 요청 → 온라인 결제 → 예약 확정 | 신청 → 온라인 결제 → 심사 → 승인(예약확정) / 반려(전액 환불) |
| [요구] | 3장 범위와 전제 | "예약은 결제 완료 시 확정" | "결제 완료 시 신청접수, 관리자 승인 시 예약확정" |
| [요구] | 4장 용어 | 승인 = 결제 허용, 임시확보 = 승인 후 결제기한까지 | 승인 = 예약확정, 임시확보 = 결제 시작부터 심사 종료까지 |
| [요구] | 6·7장 흐름·상태 전이 | 4-C 승인 → 결제대기 | 2.3 상태 모델로 교체 |
| [요구] | 15장 신청서 데이터 | 사업자/고유번호(선택) | 번호 종류 선택 + 형식·검증번호 검사, 단체 판별 키로 사용(필수 여부는 P-03) |
| [요구] | 10장 BR-01 | 매주 월요일, 1월 1일, 설·추석 당일(고정) | "관리자가 설정한 휴관 규칙(정기 요일·매년·특정일·예외 개관일)" |
| [요구] | 10장 BR-03·04 | "한 단체" | "같은 고유번호·사업자등록번호" |
| [요구] | 17장 결제 | 승인 시 결제기한 48시간 | 신청 제출 시 결제, 결제 유효시간(P-05) |
| [요구] | 18장 취소·환불 | — | 반려 시 자동 전액 환불, 승인 전 철회 환불(P-15) 추가 |
| [요구] | 21장 알림 | 승인/결제 요청 알림, 결제기한 임박 알림 | **결제 완료·신청접수**(신청자·담당자) / **승인·예약확정**(이용규정·연락처 포함) / **반려·환불**(사유·환불 금액) / 결제 유효시간 임박 알림은 PG 방식에선 삭제, 계좌이체 방식에선 유지 |
| [요구] | 26장 인수 테스트 | AT-01·06·07·08 | 5장 변경안으로 |
| [WF] | 2쪽 설계 원칙 | "예약확정은 결제 완료 시점" | "예약확정은 결제 완료 후 관리자 승인 시점" |
| [WF] | U-05(7쪽) | "대관 신청이 승인되었습니다 → 결제" | 신청서 제출 직후 결제 화면으로, 문구 "결제하면 신청이 접수됩니다" |
| [WF] | U-04(6쪽) | 규정 동의 → 신청 제출 → 신청 완료 | 규정 동의 → **결제** → 신청 완료 |
| [WF] | A-02(10쪽) | 버튼 "승인·결제요청", "승인 시 결제기한 지정" | 버튼 "승인(예약확정)", "반려(전액 환불)" + 결제 상태·단체번호 이력 표시 |
| [WF] | A-03(11쪽) | 일정 차단 종류에 "휴관일" | 휴관 규칙 설정(정기 요일·매년·특정일·예외 개관일) 영역 추가 |
| [시안] | 1·3·6쪽 흐름 | 관리자 승인 → 결제 → 예약확정 | 신청 → 결제 → 관리자 심사 → 예약확정 |
| [시안] | 5쪽 "승인 후 결제" | 승인은 결제 요청 단계로 전환 | 승인하면 곧바로 예약확정 |
